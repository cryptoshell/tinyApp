<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
    <header>
      <% include ./partials/_header %>
    </header>
    <h1>TinyApp</h1>
    <h3>URLs:</h3>
    <ul>
      <% for(var url in urls) { %>
        <li><%= url %> &#8594 <%= urls[url] %>
        <form method="POST" action='/urls/<%= url %>' style="display: inline-block">
          <a href="/urls/<%= url %>">Edit</a>
        </form>
        <form method="POST" action='/urls/<%= url %>/delete' style="display: inline-block">
          <input type="submit" value="Delete" style="background-color: lightgreen; width: 50px"></input>
        </form></li>
      <% } %>
    </ul>
  </body>
</html>